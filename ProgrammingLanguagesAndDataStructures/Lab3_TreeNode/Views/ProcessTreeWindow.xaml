<Window x:Class="Lab3_Tree.Views.ProcessTreeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Process Tree" Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#C8E6C9">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Copy tree nodes to list by key range:" 
                  FontSize="14" Foreground="#1B5E20" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Min Key:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#1B5E20"/>
            <TextBox x:Name="txtMinKey" Width="60" FontSize="14" Padding="4"/>
            <TextBlock Text="Max Key:" VerticalAlignment="Center" Margin="20,0,10,0" Foreground="#1B5E20"/>
            <TextBox x:Name="txtMaxKey" Width="60" FontSize="14" Padding="4"/>
            <Button Content="Copy" Margin="20,0,0,0" Padding="15,5" Background="#4CAF50" Foreground="White"
                    Click="Copy_Click"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Tree (In-Order):" Margin="0,0,0,10" Foreground="#1B5E20">
            <TextBlock x:Name="txtTree" FontFamily="Consolas" FontSize="12" 
                      TextWrapping="Wrap" Background="White" Padding="5"/>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Result List:" Margin="0,0,0,10" Foreground="#1B5E20">
            <TextBlock x:Name="txtList" FontFamily="Consolas" FontSize="12" 
                      TextWrapping="Wrap" Background="White" Padding="5"/>
        </GroupBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" Width="80" IsCancel="True" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>