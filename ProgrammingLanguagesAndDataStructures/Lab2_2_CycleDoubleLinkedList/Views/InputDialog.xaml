<Window x:Class="Lab2_DoubleCycleList.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="160" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15" Background="#C8E6C9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtPrompt" Text="Enter value:" FontSize="14" Margin="0,0,0,8" Foreground="#1B5E20"/>

        <TextBox x:Name="txtInput" Grid.Row="1" FontSize="14" Padding="4"
                 KeyDown="txtInput_KeyDown"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="70" Margin="0,0,10,0" IsDefault="True"
                    Background="#4CAF50" Foreground="White" FontWeight="SemiBold" Click="OK_Click"/>
            <Button Content="Cancel" Width="70" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
